<Window x:Class="CloudTopSupport.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CloudTopSupport"
        mc:Ignorable="d"
        SizeToContent="Height"
        ResizeMode="CanMinimize"
        Title="云顶之奕辅助" Height="450" Width="800"
        Topmost="True">
    <Window.Resources>
        <Style TargetType="Button">
            <Style.Setters>
                <Setter Property="Margin" Value="10"/>
                <!--<Setter Property="Margin" Value="10"/>-->
            </Style.Setters>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid Height="80">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="装备合成(精简版)" Click="EquipmentSmartButton_Click"></Button>
            <Button Grid.Column="1" Content="装备合成(完整版)" Click="EquipmentButton_Click"></Button>
            <Button Grid.Column="2" Content="阵容推荐"></Button>
            <Button Grid.Column="3" Content="英雄查看"></Button>
        </Grid>
    </Grid>
</Window>
